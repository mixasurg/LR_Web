{% extends 'base.html.twig' %}

{% block title %}{{ work.title }} | Mixas Art Works{% endblock %}

{% block body %}
    <section class="p-4 rounded-4 bg-white shadow-sm mb-4">
        <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-3">
            <h1 class="h3 mb-0">{{ work.title }}</h1>
            <a href="{{ path('app_gallery') }}" class="btn btn-sm btn-outline-dark">Назад к галерее</a>
        </div>
        <div class="page-content mb-0">{{ work.description|raw }}</div>
    </section>

    <section class="p-4 rounded-4 bg-white shadow-sm">
        {% if photos|length > 0 %}
            <div id="workCarousel{{ work.id }}" class="carousel slide" data-bs-ride="carousel">
                <div class="carousel-inner">
                    {% for photo in photos %}
                        <div class="carousel-item {{ loop.first ? 'active' : '' }}">
                            <img src="{{ asset(photo.imagePath) }}" class="d-block w-100 work-photo enlargeable-image" alt="{{ photo.caption ?: work.title }}" data-enlargeable data-fullsrc="{{ asset(photo.imagePath) }}">
                            {% if photo.caption %}
                                <div class="carousel-caption d-none d-md-block">
                                    <span class="badge text-bg-dark">{{ photo.caption }}</span>
                                </div>
                            {% endif %}
                        </div>
                    {% endfor %}
                </div>
                {% if photos|length > 1 %}
                    <button class="carousel-control-prev" type="button" data-bs-target="#workCarousel{{ work.id }}" data-bs-slide="prev">
                        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                        <span class="visually-hidden">Previous</span>
                    </button>
                    <button class="carousel-control-next" type="button" data-bs-target="#workCarousel{{ work.id }}" data-bs-slide="next">
                        <span class="carousel-control-next-icon" aria-hidden="true"></span>
                        <span class="visually-hidden">Next</span>
                    </button>
                {% endif %}
            </div>
        {% else %}
            <p class="text-muted mb-0">Для этой работы пока нет опубликованных фото.</p>
        {% endif %}
    </section>
{% endblock %}
